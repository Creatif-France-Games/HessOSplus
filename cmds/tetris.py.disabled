import curses
import time
# Simule le démarrage du jeu (placeholder)
def main(stdscr):
    stdscr.clear()
    stdscr.addstr(0, 0, "Tetris started. Press any key to quit.")
    stdscr.refresh()
    stdscr.getch()
if __name__ == "__main__":
    curses.wrapper(main)